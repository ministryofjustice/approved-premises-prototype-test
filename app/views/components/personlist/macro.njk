{# Pass values into macro (params): xxxTitle, xxxBody, xxxLinkText, xxxHref #}
{# body can be HTML, use | safe when passing #}
{% macro personList(params) %}
    {%- include "./template.njk" -%}
{% endmacro %}
